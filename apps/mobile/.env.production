# Mobile production env — committed so external users can build a personal
# iPhone copy of Lumen against the same backend as lumen.ai on web.
# Loaded by the `*:prod` scripts via dotenv-cli (see package.json).
EXPO_PUBLIC_API_URL=https://api.lumen.ai
EXPO_PUBLIC_WEB_URL=https://lumen.ai
