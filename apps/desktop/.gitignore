node_modules
dist
out
.DS_Store
.eslintcache
*.log*
# CLI binary bundled at build time (from server/bin/)
resources/bin/
# Keep any developer-local Electron distribution out of git. Packaging uses
# electron-builder's platform-aware standard cache instead of this directory.
build/electron-cache/
