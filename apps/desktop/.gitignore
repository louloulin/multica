node_modules
dist
out
.DS_Store
.eslintcache
*.log*
# CLI binary bundled at build time (from server/bin/)
resources/bin/
# Local cached Electron dist unpacked by `electronDist`; regenerated on
# demand before `pnpm run package` from the registry mirror.
build/electron-cache/
